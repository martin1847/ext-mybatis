pluginManagement {
    repositories {
//        maven{
//            url "$MAVEN_REPO"
//            credentials(HttpHeaderCredentials) {
//                name = "X-JFrog-Art-API"
//                value = System.getenv("JCR_TOKEN" )?:"$JCR_TOKEN"
//            }
//            authentication {
//                header(HttpHeaderAuthentication)
//            }
//        }
        gradlePluginPortal()
    }
}


include 'mybatis','ext-mybatis','ext-mybatis-deployment'
// native-it: native-image smoke consumer app (verifies ext-mybatis native augmentation +
// real PG mapper round-trip). Not published — root build.gradle skips upload.gradle for it.
include 'native-it'
